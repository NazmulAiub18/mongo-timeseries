{
  "name": "mongo-timeseries",
  "version": "1.0.0",
  "description": "no description",
  "main": "build/index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "seed:gen": "npm run build && node build/seed/generate.js",
    "seed": "npm run build && node build/seed/generate.js && node build/seed/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.19",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mongoose": "^8.1.3"
  }
}
